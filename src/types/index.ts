import { StaticImageData } from "next/image";

export interface StructureColsProps {
    header : string;
    paragph1 : string;
    paragph2 : string;
    span : string;
    image: StaticImageData;
}